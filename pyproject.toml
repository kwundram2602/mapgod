[project]
name = "mapgod"
version = "0.1.0"
description = "Vector + Raster maps, made simple"
requires-python = ">=3.13"
dependencies = [
    "geopandas",
    "rasterio",
    "matplotlib",
    "contextily",
    "leafmap",
    "folium",
    "numpy",
    "xarray>=2026.4.0",
    "rioxarray>=0.22.0",
    "vecraspy",
    "pillow>=12.2.0",
    "imageio>=2.37.3",
    "imageio-ffmpeg>=0.6.0",
]

[project.scripts]
install-local = "scripts.install_local:main"
add-git-ref = "scripts.add_git_ref:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mapgod", "scripts"]

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.uv.sources]
vecraspy = { git = "https://github.com/kwundram2602/vecraspy" }

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.12",
    "ty>=0.0.32",
]
